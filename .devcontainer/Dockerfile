# Use an official Node.js runtime as a parent image
FROM mcr.microsoft.com/devcontainers/typescript-node:18

# Set the working directory in the container
WORKDIR /workspace

# Install project dependencies globally if needed, or rely on postCreateCommand
# Example: RUN npm install -g pnpm

# Optional: Install other tools (e.g., git)
# RUN apt-get update && export DEBIAN_FRONTEND=noninteractive && apt-get -y install --no-install-recommends git

# Clean up
# RUN apt-get autoremove -y && apt-get clean -y && rm -rf /var/lib/apt/lists/*

# Set a non-root user
# USER node

# Keep container running
# CMD ["sleep", "infinity"]
